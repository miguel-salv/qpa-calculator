name = "cmu-qpa"
compatibility_date = "2023-10-30"

# Static site — the repo root is served directly as plain HTML/CSS/JS.
# No build step, no framework. Cloudflare Pages just uploads these assets.
pages_build_output_dir = "."
